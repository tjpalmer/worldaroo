{
  "name": "worldaroo",
  "version": "0.0.1",
  "license": "MIT",
  "dependencies": {
    "three": "^0.91.0",
    "typescript": "^2.7.2"
  },
  "devDependencies": {
    "@types/three": "^0.89.12",
    "del-cli": "^1.1.0",
    "parcel-bundler": "^1.6.2"
  },
  "scripts": {
    "prepare": "del-cli docs && parcel build index.html --out-dir docs --public-url ./",
    "start": "del-cli bin && parcel index.html --out-dir bin"
  }
}
